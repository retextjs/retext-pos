{
  "name": "retext-pos",
  "version": "0.1.2",
  "description": "Retext POS (part-of-speech) tagger",
  "license": "MIT",
  "keywords": [
    "part",
    "of",
    "speech",
    "pos",
    "tagger",
    "retext"
  ],
  "dependencies": {
    "pos": "^0.1.7"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/wooorm/retext-pos.git"
  },
  "author": "Titus Wormer <tituswormer@gmail.com>",
  "devDependencies": {
    "eslint": "^0.8.2",
    "istanbul": "^0.3.0",
    "jscs": "^1.6.0",
    "mocha": "^1.21.0",
    "retext": "^0.2.0-rc.2",
    "retext-content": "^0.2.2",
    "retext-visit": "^0.1.1"
  },
  "scripts": {
    "test": "node_modules/.bin/_mocha --reporter spec --check-leaks -u exports spec/retext-pos.spec.js",
    "test-travis": "node_modules/.bin/istanbul cover node_modules/.bin/_mocha --report lcovonly -- --reporter spec --check-leaks -u exports spec/retext-pos.spec.js",
    "lint": "npm run lint-api && npm run lint-test && npm run lint-style",
    "lint-api": "node_modules/.bin/eslint index.js --env node --env browser --rule 'quotes: [2, single]'",
    "lint-test": "node_modules/.bin/eslint spec/retext-pos.spec.js --env node --env mocha --rule 'quotes: [2, single]'",
    "lint-style": "node_modules/.bin/jscs index.js spec/retext-pos.spec.js --reporter=inline",
    "install-browser-test": "npm install browserify",
    "build-browser-test": "node_modules/.bin/browserify spec/retext-pos.spec.js -o spec/browser.spec.js",
    "coverage": "node_modules/.bin/istanbul cover node_modules/.bin/_mocha -- -- spec/retext-pos.spec.js",
    "make": "npm run lint && npm run coverage"
  }
}
